# APP Version Manager

一个 Obsidian 插件，用于管理 APP 版本规划和关联的项目信息。

## 功能特性

### 多 APP 管理
- 创建、重命名、删除 APP
- 每个 APP 可包含多个版本

### 版本管理
- 创建版本（包含 APP版本号、BLL版本、IPP版本、Web版本）
- 版本归档/取消归档
- 删除版本

### 项目管理
- 创建项目并关联到版本
- 项目属性：名称、项目经理、项目链接、组件库链接、需求描述
- 项目进度跟踪（6 个阶段）
- 提测计划配置（B1-B4 集成测试/系统测试时间）
- 逾期提醒（下一阶段时间是今天或明天时红色高亮）

### 多视图展示
- **双栏视图**: 左侧版本列表，右侧项目列表
- **看板视图**: 按进度分列的看板形式
- **表格视图**: 传统表格形式，点击进度可直接推进到下一阶段

### 智能排序
- 下一阶段时间是今天的项目排在最前
- 下一阶段时间是明天的项目次之
- 普通项目按下一阶段时间升序排列
- 已发布项目始终显示在最后

### 项目备忘录
- 双击项目卡片打开独立的备忘录文件
- 备忘录与项目配置完全隔离存储
- 可自由记录项目相关信息

### 筛选与搜索
- 按进度筛选
- 关键词搜索（项目名称、项目经理、需求）
- 保存筛选条件（直接使用搜索内容作为名称）

### 数据导入导出
- 导出为 CSV / Excel 格式
- 从 CSV / Excel 导入数据

### 数据备份
- 支持配置备份路径（默认为笔记根目录）
- 支持手动备份功能
- 自动备份功能

### 数据存储配置
- 可自定义数据存储路径（默认为 `app-version-manager`）
- 支持相对路径（相对于vault根目录）和绝对路径
- 如果指定路径不存在，插件会自动创建必要的文件夹结构
- 支持在设置中修改存储路径，插件会自动迁移数据

## 项目进度阶段

```
需求分解 → 配置组件填写 → 组件上传 → 自测验证 → 已提测 → 已发布
```

### 进度推进
- 在表格视图中，点击项目进度徽章可直接推进到下一阶段
- 弹出确认窗口显示当前进度和下一进度
- 已发布项目无法继续推进

## 快捷键

- `Ctrl+Shift+V` - 打开 APP Version Manager
- `Ctrl+Shift+N` - 创建新版本
- `Ctrl+Alt+N` - 创建新项目

## 数据存储

所有数据以 Markdown 文件形式存储在 Obsidian vault 的 `app-version-manager/` 目录下：

```
app-version-manager/
├── apps/        # APP 数据
├── versions/    # 版本数据
├── projects/    # 项目数据
├── memos/       # 项目备忘录
└── backups/     # 备份数据
```

## 安装

### 手动安装
1. 下载 `main.js`、`manifest.json` 文件
2. 在 Obsidian vault 中创建 `.obsidian/plugins/obsidian-app-version-manager/` 目录
3. 将下载的文件放入该目录
4. 重启 Obsidian 并在设置中启用插件

### 开发构建
```bash
npm install
npm run build
```

## 开发

```bash
# 开发模式（监听文件变化）
npm run dev

# 生产构建
npm run build

# 构建并部署到 Obsidian 插件目录
npm run deploy
```

## 更新日志

### v1.1.0 (2025-03-22)

#### 新功能
- **进度快速推进**: 表格视图中点击进度徽章可直接推进到下一阶段，无需右键菜单
- **备份路径配置**: 支持自定义备份文件存储路径
- **手动备份**: 设置页面添加手动备份按钮
- **日期格式支持**: 提测时间输入支持 "MM.DD" 格式，自动补全年份

#### 优化改进
- **智能排序**: 已发布项目在各视图中始终显示在最后
- **高亮逻辑优化**: 下一阶段时间是今天或明天时高亮提示（而非逾期提示）
- **弹窗按钮统一**: 所有弹窗按钮顺序统一为"确认在左，取消在右"
- **弹窗样式优化**: 进度确认弹窗使用自定义样式，显示进度对比
- **输入框宽度统一**: 弹窗中所有输入框宽度统一为 280px
- **搜索提示优化**: 搜索框提示改为"搜索项目、项目经理、项目需求..."

#### 移除功能
- 移除"计划发布时间"字段（所有视图）
- 移除表格视图中的版本和项目经理筛选功能

#### Bug 修复
- 修复删除筛选条件后输入框无法聚焦的问题
- 修复排序逻辑中已发布项目可能被覆盖的问题

## 许可证

MIT
